# Python - Environment & First Programs

This project focuses on understanding Python execution modes (interactive REPL vs script files), using virtual environments, and managing packages via pip.

## Tasks
* **1. Deterministic Script Output**: Created an executable script `structured_output.py` that outputs exact formatted results including version, numeric/float approximation, and boolean comparisons.